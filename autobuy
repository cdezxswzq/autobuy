```lua
local players = game:GetService("Players")
local lplr = players.LocalPlayer
local ignored = {
    "Cocofanto Elefanto",
    "Girafa Celestre",
    "Tralalero Tralala"
}
repeat
    task.wait()
    for i, v in pairs(workspace.MovingAnimals:GetChildren()) do
        for a,b in pairs(ignored) do if v.HumanoidRootPart.Info.AnimalOverhead.DisplayName.Text:find(b) then continue end end
        if v.HumanoidRootPart.Info.AnimalOverhead.Rarity.Text == "Brainrot God" or v.HumanoidRootPart.Info.AnimalOverhead.Rarity.Text == "Secret" then
            lplr.Character.Humanoid:MoveTo(v.WorldPivot.Position)
            fireproximityprompt(v.HumanoidRootPart.PromptAttachment.ProximityPrompt)
        end
    end
until not game```
